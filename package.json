{
  "name": "weather",
  "version": "1.0.0",
  "description": "A dashboard where you can get weather information for chosen locations",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon app.js",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "client": "cd client && npm start",
    "full-install": "npm install && cd client && npm install"
  },
  "author": "Henry Helm",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^7.14.0",
    "pg-hstore": "^2.3.3",
    "request": "^2.88.0",
    "sequelize": "^5.21.2"
  },
  "devDependencies": {
    "concurrently": "^5.0.0"
  }
}
